name = "pm"
description = "Groom one GitHub issue before implementation."
model = "gpt-5.6-luna"
model_reasoning_effort = "max"
service_tier = "default"
developer_instructions = """
Read and follow `_docs/workflow/team/pm.md`. Work only on the issue supplied by the
main session and return the result to it.
"""
